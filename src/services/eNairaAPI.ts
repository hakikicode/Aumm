export const eNairaDeposit = async (amount, walletAddress) => {
    // API call to eNaira system
    return { success: true, amount };
  };
  
  export const eNairaWithdraw = async (amount, walletAddress) => {
    // API call to withdraw funds to eNaira
    return { success: true, amount };
  };
  